pipeline{
	agent any
	stages {
		stage('docker build') {
			steps {
				echo 'docker build'
			}	
		}
		stage('docker variable defined'){
			steps {
				echo 'docker variable defined'
			}		
		}
		stage('docker artifects') {
			steps {
				echo 'docker artifects'
			}		
		}

	}
}
